// MALINDRA PHASE 1
// app/archive/layout.tsx
// Archive section uses the same layout structure as blog — no standalone nav.

import type { ReactNode } from 'react';

export default function ArchiveLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
